README:

How to run game:
	
	make sure pygame is installed, if not download pygame and put it in the tpfinal folder
	open tpfinal folder and it component(py files) in your python IDE
	make sure a ps4 controller is plugged into the usb port
	select 'gameJoyStickVzn' file and hit run
	